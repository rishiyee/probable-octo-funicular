/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you had experimental/appDir or other stuff, we can add it back later.
  // For now, we keep config minimal and valid.
  // ❗ No NODE_ENV here, Next sets it automatically.
};

module.exports = nextConfig;
